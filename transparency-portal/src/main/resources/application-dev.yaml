spring:
  datasource:
    url: jdbc:h2:mem:testdb
    username: sa
    password:
    driverClassName: org.h2.Driver

  jpa:
    database-platform: org.hibernate.dialect.H2Dialect
    defer-datasource-initialization: true
    hibernate:
      #ddl-auto: update --after development, uncomment if neede
      ddl-auto: create-drop

  h2:
    console:
      enabled: true
      path: /h2-console
      settings:
        web-allow-others: true

  security:
    oauth2:
      client:
        registration:
          google:
            client-id: ${GOOGLE_CLIENT_ID:safihbss5v2ci92c587ib.apps.googleusercontent.com}
            client-secret: ${GOOGLE_CLIENT_SECRET:fjbsfGOCSsfsfhDcoWfsdfnwuaLRHgzeyx}
            scope: openid,profile,email
            redirect-uri: "http://localhost:8080/transparency-portal/login/oauth2/code/google"